type Query {
    greeting: String
    students: [Student]
    studentByID(id: ID!): Student
}

type Student {
    id: ID!
    firstName: String
    lastName: String
    password: String
    collegeId: String
}